# This script sets up the final best-fitting model for predicting
# Bromus population growth as a function of snow cover, winter rain precipitation,
# and density (which is held constant at 0).

# The model uses an updated data file provided by Alden Griffith and Michael Loic